<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sad490.smartscrape">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme_new">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MainTheme"/>
        <activity android:name=".SignupActivity"
            android:theme="@style/MainTheme"/>
        <activity android:name=".InfoSetting"
            android:theme="@style/MainTheme"/>
        <activity android:name=".PosterDetail"
            android:theme="@style/MainTheme"/>
        <activity android:name=".BlogViewer"
            android:theme="@style/MainTheme"/>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".UserInfo.ChagePersonActivity"/>
    </application>

</manifest>